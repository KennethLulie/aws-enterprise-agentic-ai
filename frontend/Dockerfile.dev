FROM node:20-alpine

WORKDIR /app

# Install dependencies first for layer caching
COPY package*.json ./
RUN npm install

EXPOSE 3000

# Hot reload dev server (code mounted via volume)
CMD ["npm", "run", "dev"]
